# runtime deps installed by home.activation.installPiExtensionDeps
node_modules/
# build output
dist/
# local benchmark scrape cache
.cache/
# lockfile is tracked — needed for --frozen-lockfile determinism
!**/bun.lock